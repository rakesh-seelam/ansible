- name: demo on conditions
  hosts: local
  connection: local
  tasks:
  - name: printing facts
    ansible.builtin.debug:
     msg: "{{ ansible_facts }}"
  
  - name: Checking the os 
    ansible.builtin.debug:
     msg: "Yes package manager is redhat"
    when: "{{ ansible_facts.os_family == RedHat }}"