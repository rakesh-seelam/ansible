- name: Ping the server
  hosts: web
  tasks:
  - name: Ping the web server
    ansible.builtin.ping:




# - name: ping the server
#   hosts: web
#   tasks:
#    - name: ping the web server
#      ansible.builtin.ping: